Opening the resmon.ResmonCfg on Windows should open the Windows "Resource Monitor"
with the CPU tab open. You can then type a file path into the "Associated Handlers"
search bar to find files that are locking a file.

runing `resmon` in powershell will also open the "Resource Monitor". It just won't
open to the CPU panel.
